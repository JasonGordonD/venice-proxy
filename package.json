{
  "name": "venice-proxy",
  "version": "1.0.0",
  "description": "Proxy for forwarding chat requests to Venice and handling MCP separation.",
  "main": "gateway.ts",
  "scripts": {
    "start": "ts-node gateway.ts",
    "dev": "ts-node-dev --respawn --transpile-only gateway.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^4.18.2",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.8.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
