{
  "name": "venice-proxy",
  "version": "1.0.0",
  "description": "A TypeScript proxy for forwarding chat requests to Venice with MCP filtering.",
  "main": "gateway.ts",
  "scripts": {
    "start": "ts-node gateway.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
